name: Backend CI

on:
  push:
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - name: Restore dependencies
      run: dotnet restore backend/ATM-Banking-System.sln
    - name: Build
      run: dotnet build backend/ATM-Banking-System.sln --no-restore --configuration Release
    - name: Test
      run: dotnet test backend/ATM-Banking-System.sln --no-build --configuration Release --verbosity normal
